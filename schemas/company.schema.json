{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.ai-infrastructure-digital-twin.local/company.schema.json",
  "title": "Company",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "schema_version",
    "created_at",
    "modified_at",
    "source_ids",
    "confidence_score",
    "status",
    "notes",
    "legal_name"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Immutable canonical UUID for this company, referenced by company_id fields throughout the repository."
    },
    "schema_version": {
      "const": "0.1.0",
      "description": "Version of the JSON Schema contract used to validate this record; it changes only through documented repository migrations."
    },
    "created_at": {
      "$ref": "common.schema.json#/$defs/timestamp",
      "description": "UTC date-time when this repository record was first created; this is audit metadata, not the real-world entity's founding or start date."
    },
    "modified_at": {
      "$ref": "common.schema.json#/$defs/timestamp",
      "description": "UTC date-time of the latest repository record change; it must be updated whenever a structured value or its provenance changes."
    },
    "source_ids": {
      "$ref": "common.schema.json#/$defs/source_ids",
      "description": "Non-empty set of Source Registry IDs that directly support the observed, estimated, or derived values in this record."
    },
    "confidence_score": {
      "$ref": "common.schema.json#/$defs/confidence_score",
      "description": "Evidence-strength score from 1 (unverified or speculative) to 5 (verified SEC filing), assigned under ASSUMPTIONS.md."
    },
    "status": {
      "$ref": "common.schema.json#/$defs/status",
      "description": "Current lifecycle classification for the represented entity or record; unknown is used when available evidence does not establish a state."
    },
    "notes": {
      "$ref": "common.schema.json#/$defs/notes",
      "description": "Optional analyst clarification about scope, ambiguity, estimation, or lifecycle context; null means no clarification is recorded and notes never replace structured provenance."
    },
    "legal_name": {
      "type": "string",
      "minLength": 1,
      "description": "Observed registered legal name of the corporate entity, preserving disclosed punctuation and suffixes."
    },
    "display_name": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional commonly used public name; null when no distinct display name is supported by sources."
    },
    "ticker": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional observed primary trading symbol; null for private companies or when no reliable listing is established."
    },
    "exchange": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional observed primary securities exchange associated with ticker; null when not applicable or unknown."
    },
    "jurisdiction": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional observed legal jurisdiction of incorporation, expressed consistently with the source record."
    },
    "website_url": {
      "type": [
        "string",
        "null"
      ],
      "format": "uri",
      "description": "Optional absolute URI for the company's official website; null when no official site is verified."
    }
  }
}
