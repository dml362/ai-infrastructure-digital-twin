{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.ai-infrastructure-digital-twin.local/project.schema.json",
  "title": "Project",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "schema_version",
    "created_at",
    "modified_at",
    "source_ids",
    "confidence_score",
    "status",
    "notes",
    "company_id",
    "campus_id",
    "name",
    "project_type"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Immutable canonical UUID for this discrete development or infrastructure project."
    },
    "schema_version": {
      "const": "0.1.0",
      "description": "Version of the JSON Schema contract used to validate this record; it changes only through documented repository migrations."
    },
    "created_at": {
      "$ref": "common.schema.json#/$defs/timestamp",
      "description": "UTC date-time when this repository record was first created; this is audit metadata, not the real-world entity's founding or start date."
    },
    "modified_at": {
      "$ref": "common.schema.json#/$defs/timestamp",
      "description": "UTC date-time of the latest repository record change; it must be updated whenever a structured value or its provenance changes."
    },
    "source_ids": {
      "$ref": "common.schema.json#/$defs/source_ids",
      "description": "Non-empty set of Source Document IDs that directly support the observed, estimated, or derived values in this record."
    },
    "confidence_score": {
      "$ref": "common.schema.json#/$defs/confidence_score",
      "description": "Evidence-strength score from 1 (unverified or speculative) to 5 (verified SEC filing), assigned under ASSUMPTIONS.md."
    },
    "status": {
      "$ref": "common.schema.json#/$defs/status",
      "description": "Current lifecycle classification for the represented entity or record; unknown is used when available evidence does not establish a state."
    },
    "notes": {
      "$ref": "common.schema.json#/$defs/notes",
      "description": "Optional analyst clarification about scope, ambiguity, estimation, or lifecycle context; null means no clarification is recorded and notes never replace structured provenance."
    },
    "company_id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "UUID of the responsible Company; foreign key to Company.id."
    },
    "campus_id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "UUID of the Campus where the project is located; foreign key to Campus.id."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Observed project, phase, building, or work-package name that distinguishes this delivery scope."
    },
    "project_type": {
      "type": "string",
      "enum": [
        "data_center",
        "power",
        "transmission",
        "interconnection",
        "site_infrastructure",
        "other"
      ],
      "description": "Allowed infrastructure classification describing the project's primary delivery scope."
    },
    "planned_capacity_mw": {
      "type": [
        "number",
        "null"
      ],
      "minimum": 0,
      "description": "Optional observed or estimated planned nameplate capacity in megawatts; null when capacity is not applicable or supportable."
    },
    "start_date": {
      "anyOf": [
        {
          "$ref": "common.schema.json#/$defs/date"
        },
        {
          "type": "null"
        }
      ],
      "description": "Optional observed or estimated ISO 8601 date when project execution began or is expected to begin; null when unknown."
    },
    "target_completion_date": {
      "anyOf": [
        {
          "$ref": "common.schema.json#/$defs/date"
        },
        {
          "type": "null"
        }
      ],
      "description": "Optional observed or estimated ISO 8601 target date for completion; null when no supported target exists."
    }
  }
}
