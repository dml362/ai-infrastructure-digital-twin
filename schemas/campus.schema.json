{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.ai-infrastructure-digital-twin.local/campus.schema.json",
  "title": "Campus",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "schema_version",
    "created_at",
    "modified_at",
    "source_ids",
    "confidence_score",
    "status",
    "notes",
    "company_id",
    "name",
    "country_code"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Immutable canonical UUID for this physical campus."
    },
    "schema_version": {
      "const": "0.1.0",
      "description": "Version of the JSON Schema contract used to validate this record; it changes only through documented repository migrations."
    },
    "created_at": {
      "$ref": "common.schema.json#/$defs/timestamp",
      "description": "UTC date-time when this repository record was first created; this is audit metadata, not the real-world entity's founding or start date."
    },
    "modified_at": {
      "$ref": "common.schema.json#/$defs/timestamp",
      "description": "UTC date-time of the latest repository record change; it must be updated whenever a structured value or its provenance changes."
    },
    "source_ids": {
      "$ref": "common.schema.json#/$defs/source_ids",
      "description": "Non-empty set of Source Document IDs that directly support the observed, estimated, or derived values in this record."
    },
    "confidence_score": {
      "$ref": "common.schema.json#/$defs/confidence_score",
      "description": "Evidence-strength score from 1 (unverified or speculative) to 5 (verified SEC filing), assigned under ASSUMPTIONS.md."
    },
    "status": {
      "$ref": "common.schema.json#/$defs/status",
      "description": "Current lifecycle classification for the represented entity or record; unknown is used when available evidence does not establish a state."
    },
    "notes": {
      "$ref": "common.schema.json#/$defs/notes",
      "description": "Optional analyst clarification about scope, ambiguity, estimation, or lifecycle context; null means no clarification is recorded and notes never replace structured provenance."
    },
    "company_id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "UUID of the Company that owns or operates the campus; this is a foreign key to Company.id."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Observed or company-confirmed campus name used to distinguish the physical site."
    },
    "country_code": {
      "type": "string",
      "pattern": "^[A-Z]{2}$",
      "description": "Observed ISO 3166-1 alpha-2 code for the country containing the campus."
    },
    "region": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional observed first-level region, state, or province; null when unavailable."
    },
    "city": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional observed municipality or nearest disclosed city; null when unavailable."
    },
    "latitude": {
      "type": [
        "number",
        "null"
      ],
      "minimum": -90,
      "maximum": 90,
      "description": "Optional observed or estimated WGS 84 latitude in decimal degrees from -90 to 90; null when location precision is unavailable."
    },
    "longitude": {
      "type": [
        "number",
        "null"
      ],
      "minimum": -180,
      "maximum": 180,
      "description": "Optional observed or estimated WGS 84 longitude in decimal degrees from -180 to 180; null when location precision is unavailable."
    },
    "site_area_acres": {
      "type": [
        "number",
        "null"
      ],
      "minimum": 0,
      "description": "Optional observed or estimated total campus land area in acres; null when undisclosed and not reliably estimable."
    }
  }
}
